<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wikisprint.server.mapper.ConsentMapper">

    <!-- 동의 이력 삽입 (동의한 항목만 호출. is_agreed 컬럼 없음 — row 존재 자체가 동의를 의미) -->
    <insert id="insertConsent" parameterType="ConsentRecordVO">
        INSERT INTO consent_records (account_id, consent_type, consent_version)
        VALUES (#{accountId}, #{consentType}, #{consentVersion})
    </insert>

    <!-- 계정의 동의 이력 조회 (존재하는 row = 동의한 항목들) -->
    <select id="selectConsentsByAccountId" resultType="ConsentRecordVO">
        SELECT id, account_id, consent_type, consent_version, agreed_at
        FROM consent_records
        WHERE account_id = #{accountId}
        ORDER BY agreed_at ASC
    </select>

    <!-- 회원탈퇴 처리 시 동의 이력 전체 삭제 -->
    <delete id="deleteAllByAccountId">
        DELETE FROM consent_records
        WHERE account_id = #{accountId}
    </delete>

</mapper>
